import React, {
  memo,
} from 'react';
import propTypes from "prop-types";
import "./Account.css";

const Account = memo(() => {
  return (
    <div className="account">

    </div>
  );
});

Account.propTypes = {};

export default Account;
