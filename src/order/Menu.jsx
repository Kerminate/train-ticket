import React, {
  memo,
} from 'react';
import PropTypes from "prop-types";
import "./Menu.css";

const Menu = memo(() => {
  return (
    <div className="menu">

    </div>
  );
});

Menu.propTypes = {};

export default Menu;
